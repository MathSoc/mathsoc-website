extends /views/templates/frontend-base.pug

block title
    title #{data.pageTitle} &#8211; MathSoc

append styles
    link(href="/assets/css/pages/cartoons/about.css" rel="stylesheet")

append scripts
    script(src="/assets/js/carousel.js" defer)
    script(src="https://kit.fontawesome.com/c02fb43ad1.js" crossorigin="anonymous")

block body
    div(class="large-screen")
        +banner-image(data.coverPicSrc, ["full"])
    div(class="mobile-screen")
        +banner-image(data.coverPicSrcMobile, ["full"])
    div(class="content")
        section(class="white")
            div(class="title")
                h1 #{data.heading}
            div(class="row")
                div(class="col about-us-section")
                    p!=data.subheadingCaption
                    h3 #{data.joinUs}
                    p(class="app-caption") #{data.applicationsCaption}
                    div!=data.signupMarkdown
                    div(style="width: 100%; text-align:center;")
                        +pink-button(data.buttons.team.text, '', data.buttons.team.link)
                        br
                        +pink-button(data.buttons.archive.text, '', data.buttons.archive.link)
                div(class="col")
                    - var images = data.carouselImages;
                    +carousel(data.carouselImages)
            div(class="title get-in-touch")
                h1 #{data.getInTouch}
            div(class="button-row")
                +pink-button(data.socialButtons.emailMarkdown, "email", data.socialLinks.email)
                +pink-button(data.socialButtons.feedbackMarkdown, "feedback", data.socialLinks.feedback)
                +pink-button(data.socialButtons.instagramMarkdown, "instagram", data.socialLinks.instagram)
                +pink-button(data.socialButtons.facebookMarkdown, "facebook", data.socialLinks.facebook)

